<?xml version="1.0"?>
<ruleset name="docker-plugin">
    <description>PSR-2 coding standard for docker-plugin</description>

    <!-- Files and directories to check -->
    <file>Docker.php</file>
    <file>DockerService.php</file>
    <file>DockerJobManager.php</file>
    <file>DockerHubClient.php</file>
    <file>ajax</file>

    <!-- Exclude mixed HTML/PHP templates -->
    <exclude-pattern>templates/*</exclude-pattern>
    <exclude-pattern>vendor/*</exclude-pattern>

    <rule ref="PSR2"/>

    <!-- Suppress PSR1 side-effects warning: require_once in plugin files and
         procedural ajax scripts is unavoidable in RaspAP's plugin architecture -->
    <rule ref="PSR1.Files.SideEffects">
        <severity>0</severity>
    </rule>

    <!-- Output formatting -->
    <arg name="colors"/>
    <arg value="sp"/>
    <arg name="parallel" value="4"/>
</ruleset>
